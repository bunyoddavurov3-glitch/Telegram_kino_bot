<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <title>Информация о фильме</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- SEO (neytral) -->
  <meta name="description" content="Краткая информация о фильме." />

  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
      background: #0f0f12;
      color: #eaeaf0;
    }
    .container {
      max-width: 720px;
      margin: 0 auto;
      padding: 16px;
    }
    .card {
      background: #1a1a22;
      border-radius: 14px;
      padding: 16px;
      margin-bottom: 16px;
    }
    h1 {
      font-size: 20px;
      margin: 0 0 8px;
    }
    p {
      font-size: 14px;
      line-height: 1.6;
      color: #cfcfe6;
      margin: 0;
    }
    .btn {
      display: inline-block;
      margin-top: 14px;
      padding: 10px 14px;
      background: #2b5cff;
      color: #fff;
      border-radius: 10px;
      text-decoration: none;
      font-weight: 500;
    }
    .ad {
      margin: 16px 0;
      padding: 10px;
      background: #111118;
      border-radius: 12px;
      text-align: center;
      color: #8f8fb3;
      font-size: 13px;
    }
    footer {
      text-align: center;
      font-size: 12px;
      color: #8f8fb3;
      margin-top: 24px;
    }
  </style>
</head>
<body>
  <div class="container">

    <!-- FILM INFO -->
    <div class="card">
      <h1 id="title">Название фильма</h1>
      <p id="desc">
        Краткое описание фильма. Здесь только информационный текст без
        ссылок на просмотр или загрузку.
      </p>

      <!-- BOTGA QAYTISH -->
      <a class="btn" id="backBtn" href="#">⬅️ Вернуться в бот</a>
    </div>

    <!-- YANDEX ADS ZONA -->
    <div class="ad">
      <!-- 🔽 BU JOYGA YANDEX ADS KODI QO‘YILADI 🔽 -->
      <!-- Yandex Ads script shu yerda bo‘ladi -->
      Рекламный блок
    </div>

    <footer>
      © Информационная страница
    </footer>
  </div>

  <script>
    // URL parametrlaridan ma'lumot olish
    const params = new URLSearchParams(window.location.search);

    const title = params.get("title");
    const desc  = params.get("desc");
    const bot   = params.get("bot");

    if (title) document.getElementById("title").innerText = title;
    if (desc)  document.getElementById("desc").innerText  = desc;
    if (bot)   document.getElementById("backBtn").href    = bot;
  </script>
</body>
</html>

